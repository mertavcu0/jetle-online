const API = "";  function escapeHtml(value) {   return String(value || "")     .replace(/&/g, "&amp;")     .replace(/</g, "&lt;")     .replace(/>/g, "&gt;")     .replace(/"/g, "&quot;")     .replace(/'/g, "&#039;"); }  function fixMojibakeText(value) {   const text = String(value ?? "");   if (!/[\u00C3\u00C2\u00C4\u00C5\uFFFD]/.test(text)) return text;   try {     const bytes = Uint8Array.from(Array.from(text).map((ch) => ch.charCodeAt(0) & 255));     const decoded = new TextDecoder("utf-8").decode(bytes);     return decoded && decoded !== text ? decoded.replace(/\uFFFD/g, "") : text.replace(/\uFFFD/g, "");   } catch (_) {     return text.replace(/\uFFFD/g, "");   } }  function normalizeTextValue(value) {   if (Array.isArray(value)) return value.map((item) => normalizeTextValue(item));   if (typeof value === "string") return fixMojibakeText(value).normalize("NFC");   return value; }  function normalizeTextObject(value) {   if (Array.isArray(value)) return value.map((item) => normalizeTextObject(item));   if (value && typeof value === "object") {     const next = {};     Object.entries(value).forEach(([key, item]) => {       next[key] = normalizeTextObject(item);     });     return next;   }   return normalizeTextValue(value); }   function getParams() {   return new URLSearchParams(window.location.search); }  const categoryLabels = {   emlak: "Emlak",   vasita: "Vasıta",   "is-makineleri": "İş Makineleri",   "yedek-parca": "Yedek Parça" };  function normalizeCategorySlug(value) {   const raw = String(value || "").trim().toLowerCase();   if (!raw) return "";    const normalized = raw     .normalize("NFD")     .replace(/[\u0300-\u036f]/g, "")     .replace(/ı/g, "i")     .replace(/[^a-z0-9]+/g, "-")     .replace(/^-+|-+$/g, "");    if (normalized === "emlak") return "emlak";   if (["vasita", "otomobil", "arac", "araba"].includes(normalized)) return "vasita";   if (["is-makineleri", "is-makinalari", "is-makinasi", "is-makinesi"].includes(normalized)) return "is-makineleri";   if (["yedek-parca", "yedek-parca-ve-aksesuar", "yedek-parca-aksesuar"].includes(normalized)) return "yedek-parca";   return normalized; }  function getCategoryFromUrl() {   return normalizeCategorySlug(getParams().get("category") || ""); }  function setPageTitle() {   const title = document.getElementById("pageTitle");   if (!title) return;    const category = getCategoryFromUrl();   title.textContent = category     ? `${categoryLabels[category] || category} İlanları`     : "Tüm İlanlar"; }  function setFormFromUrl() {   const params = getParams();    const searchInput = document.getElementById("searchInput");   const category = document.getElementById("filterCategory");   const city = document.getElementById("filterCity");   const min = document.getElementById("minPrice");   const max = document.getElementById("maxPrice");    if (searchInput) searchInput.value = params.get("q") || params.get("search") || "";   if (category) category.value = normalizeCategorySlug(params.get("category") || "");   if (city) city.value = params.get("city") || "";   if (min) min.value = params.get("min") || "";   if (max) max.value = params.get("max") || ""; }  function buildQueryFromForm() {   const params = new URLSearchParams();   const search = document.getElementById("searchInput")?.value.trim();   const category = document.getElementById("filterCategory")?.value;   const city = document.getElementById("filterCity")?.value.trim();   const min = document.getElementById("minPrice")?.value;   const max = document.getElementById("maxPrice")?.value;    if (search) params.set("q", search);   if (category) params.set("category", normalizeCategorySlug(category));   if (city) params.set("city", city);   if (min) params.set("min", min);   if (max) params.set("max", max);    return params; }  function getListingBadges(listing) {   const badges = Array.isArray(listing.user?.badges) ? listing.user.badges : [];   const legacyBadge = listing.user?.badge && listing.user.badge !== "none"     ? [listing.user.badge]     : [];    return [...new Set([...badges, ...legacyBadge])].filter((badge) =>     ["verified", "premium", "corporate"].includes(badge)   ); }  function renderBadges(listing) {   const labels = {     verified: "Onaylı",     premium: "Premium",     corporate: "Kurumsal"   };   const badges = getListingBadges(listing);    if (!badges.length) return "";    return `     <div class="badge-row">       ${badges.map((badge) => `<span class="seller-badge ${badge}">${labels[badge]}</span>`).join("")}     </div>   `; }  function isPlaceholderImage(src) {   const value = String(src || "").trim().toLowerCase();   if (!value) return true;   return value.includes("picsum.photos") || value.includes("images.unsplash.com") || value.includes("source.unsplash.com"); }  function getListingImages(listing) {   const values = [];   if (Array.isArray(listing?.images)) values.push(...listing.images);   if (Array.isArray(listing?.photos)) values.push(...listing.photos);   if (listing?.image) values.push(listing.image);    return [...new Set(values.map((item) => String(item || "").trim()).filter((item) => item && !isPlaceholderImage(item)))]; }  function listingImage(listing) {   return getListingImages(listing)[0] || ""; }  function formatPrice(price) {   return new Intl.NumberFormat("tr-TR").format(Number(price || 0)) + " TL"; }  function currentUser() {   try {     return normalizeTextObject(JSON.parse(localStorage.getItem("user") || "{}"));   } catch (err) {     return {};   } }  function userIdentity(user) {   return user?._id || user?.id || user?.email || ""; }  function isFavorite(listing, user) {   const identity = userIdentity(user);   const email = user?.email || "";   const favorites = Array.isArray(listing.favorites) ? listing.favorites : [];    return favorites.some((favorite) => {     if (!favorite) return false;     if (typeof favorite === "object") {       return String(favorite._id || favorite.id || "") === String(identity) ||         String(favorite.email || "") === String(email);     }     return String(favorite) === String(identity);   }); }  function favoriteButton(listing, user) {   const active = isFavorite(listing, user);   const id = listing?._id || listing?.id || "";   return `     <button class="fav-btn ${active ? "active" : ""}" type="button" data-action="favorite" data-id="${escapeHtml(id)}">       ${active ? "♥" : "♡"}     </button>   `; }  function renderListings(listings) {   const container = document.getElementById("listingContainer");   if (!container) return;    if (!Array.isArray(listings) || !listings.length) {     const activeCategory = getCategoryFromUrl();     container.innerHTML = `       <div class="empty-state">         <h3>İlan bulunamadı</h3>         <p>${activeCategory ? "Bu kategoride henüz ilan yok." : "Filtreleri değiştirerek tekrar arama yapabilirsiniz."}</p>       </div>     `;     return;   }    const user = currentUser();    container.innerHTML = listings.map((listing) => {     const src = listingImage(listing);      return `       <div class="listing-card" data-action="detail" data-id="${escapeHtml(String(listing._id || listing.id || ""))}">         ${src           ? `<img class="card-img" src="${escapeHtml(src)}" alt="${escapeHtml(listing.title || "İlan")}" loading="lazy">`           : `<div class="card-img-placeholder">Görsel yok</div>`}         ${favoriteButton(listing, user)}         <div class="card-body">           <h3>${escapeHtml(listing.title || "")}</h3>           <p>${formatPrice(listing.price)}</p>           <span>${escapeHtml([listing.city, listing.district].filter(Boolean).join(" / "))}</span>           ${renderBadges(listing)}         </div>       </div>     `;   }).join(""); }  async function loadListings() {   setPageTitle();    const container = document.getElementById("listingContainer");   if (container) {     container.innerHTML = `       <div class="loading-state">         <div class="spinner"></div>         İlanlar yükleniyor...       </div>     `;   }    const params = getParams();   const res = await fetch(`${API}/api/listings${params.toString() ? `?${params.toString()}` : ""}`);   const data = await res.json();   const activeCategory = getCategoryFromUrl();   const mergedListings = (Array.isArray(data) ? data : []).map((listing) => normalizeTextObject(listing));   const filteredListings = activeCategory     ? mergedListings.filter((listing) => normalizeCategorySlug(listing?.category) === activeCategory)     : mergedListings;    renderListings(filteredListings); }  async function toggleFavorite(id) {   const user = currentUser();   const token = String(localStorage.getItem("token") || "").trim();    if ((!user.email && !user.id && !user._id) || !token) {     alert("Favorilere eklemek için giriş yapmalısınız");     window.location.href = "/login.html";     return;   }    const res = await fetch(`/api/listings/${id}/favorite`, {     method: "POST",     headers: {       Authorization: `Bearer ${token}`     }   });    if (res.status === 401 || res.status === 403) {     localStorage.removeItem("token");     localStorage.removeItem("user");     window.location.href = "/login.html";     return;   }    if (!res.ok) {     alert("Favori işlemi tamamlanamadı");     return;   }    loadListings(); }  function applyFilters() {   const params = buildQueryFromForm();   window.history.pushState({}, "", `listings.html${params.toString() ? `?${params.toString()}` : ""}`);   setPageTitle();   loadListings(); }  function debounce(fn, delay = 300) {   let timer;   return (...args) => {     clearTimeout(timer);     timer = setTimeout(() => fn(...args), delay);   }; }  function goDetail(id) {   window.location.href = `listing-detail.html?id=${encodeURIComponent(id)}`; }  document.addEventListener("DOMContentLoaded", () => {   setFormFromUrl();   loadListings();    document.getElementById("applyFilter")?.addEventListener("click", applyFilters);    const debouncedApplyFilters = debounce(applyFilters, 350);    ["filterCategory"].forEach((id) => {     document.getElementById(id)?.addEventListener("change", applyFilters);   });    ["searchInput", "filterCity", "minPrice", "maxPrice"].forEach((id) => {     const input = document.getElementById(id);     input?.addEventListener("input", debouncedApplyFilters);     input?.addEventListener("keydown", (event) => {       if (event.key === "Enter") {         event.preventDefault();         applyFilters();       }     });   });    document.addEventListener("click", (event) => {     const favoriteBtn = event.target.closest('[data-action="favorite"]');     if (favoriteBtn) {       event.preventDefault();       event.stopPropagation();       toggleFavorite(favoriteBtn.dataset.id || "");       return;     }      const detailCard = event.target.closest('[data-action="detail"]');     if (detailCard) {       goDetail(detailCard.dataset.id || "");     }   }); });